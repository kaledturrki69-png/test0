# ---------------------------------------------------------------------------
# PRODUCTION frontend environment. Copy to `.env` on the server and fill in.
#   cp env.production.example.txt .env  &&  edit .env
# ---------------------------------------------------------------------------

# NextAuth — generate a secret with: openssl rand -base64 32
NEXTAUTH_SECRET=CHANGE_ME_random_secret
NEXTAUTH_URL=https://your-frontend-domain.com

# Public app URL and Django API URL (must be HTTPS in production)
NEXT_PUBLIC_APP_URL=https://your-frontend-domain.com
NEXT_PUBLIC_API_URL=https://brain.your-domain.com

# Sentry (optional — leave disabled if unused)
NEXT_PUBLIC_SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DISABLED=true
