# Backend environment TEMPLATE. Copy to `.env` and fill in real values.
# NEVER put real secrets in this file — it is committed to git.

# --- Django core ---
DJANGO_SECRET_KEY=CHANGE_ME_generate_a_long_random_string
DJANGO_DEBUG=0
DJANGO_ALLOWED_HOSTS=your-api-domain.com
DJANGO_CORS_ALLOWED_ORIGINS=https://your-frontend-domain.com
DJANGO_CSRF_TRUSTED_ORIGINS=https://your-frontend-domain.com

# --- Database (PostgreSQL + pgvector) ---
POSTGRES_DB=brain
POSTGRES_USER=brain
POSTGRES_PASSWORD=CHANGE_ME_strong_db_password
POSTGRES_HOST=getajob-brain-db
POSTGRES_PORT=5432

# --- Redis / Celery ---
REDIS_URL=redis://getajob-brain-redis:6379/0
CELERY_BROKER_URL=redis://getajob-brain-redis:6379/1
CELERY_RESULT_BACKEND=redis://getajob-brain-redis:6379/2

# --- AI provider (OpenAI or an OpenAI-compatible endpoint like Gemini) ---
OPENAI_API_KEY=
OPENAI_BASE_URL=
OPENAI_CHAT_MODEL=gpt-4o-2024-08-06
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# --- Misc ---
DEFAULT_LANGUAGE=fr
BUILD_TOKEN=CHANGE_ME_random_build_token
FRONTEND_URL=https://your-frontend-domain.com

# --- Initial superuser (created on first deploy; leave blank to skip) ---
DJANGO_SUPERUSER_EMAIL=
DJANGO_SUPERUSER_PASSWORD=

# --- Email (SMTP) ---
DEFAULT_FROM_EMAIL=you@example.com
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=you@example.com
EMAIL_HOST_PASSWORD=CHANGE_ME_email_app_password
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False

# --- Deploy metadata (optional, set by CI) ---
DEPLOY_COMMIT=""
DEPLOY_DATE=""
DEPLOY_AUTHOR=""
DEPLOY_BRANCH=""
DEPLOY_MESSAGE=""
